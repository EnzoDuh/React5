import React from "react";

const DataAPI = ({ selectQuote }) => {
    return (
        <div className="CallQuote">
            <button onClick={selectQuote}>SMASH THAT BUTTON !</button>
        </div>
    );
};
export default DataAPI;
